# Runtime state written by the DE1app on the tablet -- personal data,
# never committed (per the plugin's packaging rules).
settings.tdb
last_recommendation.tdb
last_state.tdb

# Local backup/trash/log folders (never shipped)
backups/
trash/
logs/
